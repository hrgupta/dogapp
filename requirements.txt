# Modernized for Python 3.14 — Keras 3 (PyTorch backend) replaces TensorFlow 2.2.
# See git history for the original 2020-era pins (tensorflow==2.2.0 etc.).
fastapi>=0.115
uvicorn[standard]>=0.30
numpy>=2.0
pandas>=2.2
pillow>=11.0
tqdm>=4.66
h5py>=3.11
keras>=3.5
torch>=2.5
streamlit>=1.40
requests>=2.32
httpx>=0.27
